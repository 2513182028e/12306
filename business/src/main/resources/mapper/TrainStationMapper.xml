<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.java.train.business.mapper.TrainStationMapper">


   <select id="findByCode">
       select * from train_station where  train_code=#{trainCode} order by `index`
   </select>


</mapper>